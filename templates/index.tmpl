<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>martini gorm example</title>
</head>
<body>
    <h1>Items</h1>
    {{ range .Items }}
        <h3>{{ .Title }}</h3>
        <p>{{ .Description }}</p>
        <p>By: {{ .UserName }}</p>
    {{ else }}
        <p>No items found!</p>
    {{ end }}
</body>
</html>
